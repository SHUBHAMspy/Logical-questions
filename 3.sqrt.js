// 3. Square Root of a number
// When we multiply a number 2 times then we get square
//We can check if a number is perfect square.
function sqrt(number) {
  let root = Math.sqrt(number);
  return root
}
console.log(sqrt(10));